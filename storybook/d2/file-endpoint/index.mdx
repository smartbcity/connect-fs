import { DescriptedCode } from '@smartb/g2-documentation'
import FileEndpointDescLeft from './descLeft.md'
import FileEndpointDescRight from './descRight.md'

<DescriptedCode
  leftElement={<FileEndpointDescLeft />}
  leftContainerProps={
    {
      width: "50%"
    }
  }
  rightElement={<FileEndpointDescRight />}
  rightContainerProps={
    {
      width: "50%",
      position: "relative",
      paddingTop: "0"
    }
  }
/>

---
